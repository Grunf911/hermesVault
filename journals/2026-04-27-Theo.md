# 2026-04-27

## What we discussed
- Whether `hermes-agent` could be installed on a Raspberry Pi and constrained to do only the Luma-style cron workflow.
- The relevant Hermes/Luma docs and skills: `hermes-agent`, `luma-youtube-workflow`, and `luma-main-db-video-pages`.
- How much of the setup would depend on Notion/MainDB integration versus the core agent runtime.

## What you learned
- `hermes-agent` appears feasible on Raspberry Pi/Linux, including ARM architectures like `aarch64` and `armv7l`.
- The installer/docs indicate a Python 3.11+ requirement and support for the basic cron/skill workflow needed for Luma-like automation.
- A Pi setup should be workable for the narrow cron-based workflow, but browser-heavy or GUI-dependent tooling may still need extra care.

## Mistakes you made or wrong assumptions
- You first tried to load a non-existent skill (`autonomous-ai-agents`) before switching to the actual available skills.
- Feasibility was assessed from repository/docs inspection rather than a live Raspberry Pi install test.

## Recommended corrections to avoid repeating them
- Check the available skill list before assuming a skill name exists.
- Treat documentation-based compatibility as provisional until a live install verifies it on the target Pi.
- For future Pi guides, separate the minimal cron-only path from optional browser/GUI features so the core setup stays realistic.